SealCheckSandbox {

  sampleFile = ""
  targetFile = ""
  objSize = 30
  script = ""
  matcher.method = "ccorr_normed"
  diffBuilder.winOffset = 10

  script = """
task(preDiff);
medianBlur(2, 5)

task(buildDiff)
get(dm1); combine(dm2, absDiff)

task(postDiff)
threshold(20:dt);
findContours; filterContours(area, 300); drawContours; combine(dm2, max)
"""

}